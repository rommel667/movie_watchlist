const initialState = {
    movies: [],
    
}

const queryReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default queryReducer