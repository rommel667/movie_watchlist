const initialState = {
    favoriteMovies: []
}

const favoritesReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default favoritesReducer